@using Falcon.Web.Mvc.HtmlExtensions
@model Vino.Server.Services.MainServices.CRM.Contact.Models.CrmContactModel
@{
    Layout = "~/Areas/Admin/Views/Shared/_PopupLayout.cshtml";
    ViewBag.Title = "Tạo mới contact";
    <style>
        .k-combobox .k-input {
            height: 27.5px !important
        }
    </style>
}
@if (ViewBag.RefreshPage == true)
{
    <script type="text/javascript">
        $(document).ready(function() {
            try {
                var event = new CustomEvent("contact", { detail: { viewId: '@ViewBag.viewId', valueId: '@Model.Id'}});
                window.opener.document.forms['@(ViewBag.formId)'].@(ViewBag.btnId).dispatchEvent(event);
            } catch
            (e) {
            }
            window.close();
        })
    </script>
}
@using (Html.BeginForm(null, null, FormMethod.Post, new { enctype = "multipart/form-data", id = "popup-contact-edit-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            Tạo mới contact
        </h1>
        <div class="pull-right">
        </div>
    </div>
    @Html.Partial("~/Areas/Admin/Views/contact/_CreateContact.cshtml", Model)
}